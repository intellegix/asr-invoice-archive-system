# ASR Invoice Archive System - Docker Environment Configuration
# Copy this file to .env and configure for your environment

# =============================================================================
# API Server Configuration
# =============================================================================
DEBUG=false
API_PORT=8000
LOG_LEVEL=INFO
FRONTEND_PORT=80

# =============================================================================
# Storage Configuration
# =============================================================================
# Options: local, s3, render_disk
STORAGE_BACKEND=local
DATA_DIR=/app/data

# For S3 storage (if STORAGE_BACKEND=s3):
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-west-2
# S3_BUCKET_NAME=asr-documents

# =============================================================================
# Database Configuration
# =============================================================================
# For SQLite (development):
# DATABASE_URL=sqlite:///./asr_records.db

# For PostgreSQL (production):
DATABASE_URL=postgresql://asr_user:asr_password_change_me@postgres:5432/asr_records

# PostgreSQL container settings
POSTGRES_USER=asr_user
POSTGRES_PASSWORD=asr_password_change_me
POSTGRES_DB=asr_records
POSTGRES_PORT=5432

# =============================================================================
# Claude AI Configuration
# =============================================================================
# Required for document classification and payment detection
ANTHROPIC_API_KEY=your-anthropic-api-key-here
# Model for document analysis (default: claude-sonnet-4-5-20250929)
# CLAUDE_MODEL=claude-sonnet-4-5-20250929

# =============================================================================
# Security Configuration
# =============================================================================
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=change-me-to-a-secure-256-bit-key-in-production

# =============================================================================
# Multi-Tenant Configuration
# =============================================================================
MULTI_TENANT_ENABLED=false
DEFAULT_TENANT_ID=default

# =============================================================================
# Scanner API Configuration
# =============================================================================
SCANNER_API_ENABLED=true
MAX_SCANNER_CLIENTS=10

# =============================================================================
# Frontend Configuration
# =============================================================================
VITE_API_URL=http://localhost:8000

# =============================================================================
# Logging & Observability
# =============================================================================
# text (human-readable) or json (structured, for CloudWatch/ECS)
LOG_FORMAT=text
# Enable OpenTelemetry tracing (requires OTEL collector)
OTEL_ENABLED=false
# Show /docs and /redoc Swagger UI (requires DEBUG=true by default)
ENABLE_DOCS=false

# =============================================================================
# Redis Configuration (optional)
# =============================================================================
# Enable by running: docker-compose --profile with-redis up
REDIS_PORT=6379
# Rate limit backend: 'memory' (default) or 'redis'
# RATE_LIMIT_BACKEND=redis
# REDIS_URL=redis://localhost:6379/0
